# Evidence data

This directory contains manuscript-facing, reviewable source facts for the
pitch-deck exemplar. `claim_ledger.yaml` records slide-budget, page-ratio, and
coverage-floor claims together with the source file that defines each value.

Generated deck data belongs under `output/`, not here.
