# `data/` agent contract

Keep only stable input evidence in this directory. Every numeric manuscript
claim added to `claim_ledger.yaml` must name an authoritative source and an
active artifact path. Do not copy generated deck metrics here; derive them into
`output/` and let the evidence registry collect those artifacts directly.
